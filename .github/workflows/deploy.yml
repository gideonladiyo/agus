# name: CI/CD Pipeline Agus Bot

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy-bot:
#     name: Deploy to Production
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4
      
#       - name: Deploy via SSH
#         uses: appleboy/ssh-action@v1
#         with:
#           host: ${{ secrets.SERVER_HOST }}
#           username: ${{ secrets.SERVER_USER }}
#           password: ${{ secrets.SERVER_PASSWORD }}
#           script: |
#             echo "✅ Memastikan direktori proyek tersedia"
#             if [ ! -d "/home/gideonladiyo/PaceSorong_DiscordBot" ]; then
#                 echo "📁 Direktori belum ada, membuat direktori..."
#                 mkdir -p "/home/gideonladiyo/agus"
#             fi

#             cd /home/gideonladiyo/agus

#             if [ ! -d ".git" ]; then
#                 echo "🔄 Folder belum berisi repo Git, melakukan clone..."
#                 git clone https://github.com/gideonladiyo/agus.git .
#             fi

#             echo "🔑 Membuat file .env dari secret"
#             echo "DISCORD_TOKEN=${{ secrets.DISCORD_TOKEN }}" > .env

#             echo "📥 Menandai folder git sebagai aman"
#             git config --global --add safe.directory /home/gideonladiyo/agus

#             echo "📥 Pulling latest changes..."
#             git pull origin main

#             echo "🐳 Mengecek apakah container agus-bot sedang berjalan"
#             if [ "$(sudo docker ps -aq -f name=agus-bot)" ]; then
#                 echo "🛑 Menghentikan dan menghapus container lama..."
#                 sudo docker stop agus-bot
#                 sudo docker rm agus-bot
#             fi

#             echo "🐳 Membuild dan menjalankan kontainer Docker"
#             sudo docker build -t agus-bot .
#             sudo docker run -d \
#               --name agus-bot \
#               --env-file .env \
#               -v /home/gideonladiyo/agus/data:/app/data \
#               agus-bot

#             echo "🚀 Bot berhasil dideploy"

# ==================== railway di bawah ==================

name: Deploy to Railway

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Railway (via bervProject Action) 🚀
        uses: bervProject/railway-deploy@main
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        with:
          service: "agus" 

